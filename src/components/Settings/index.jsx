import Header from '../Header';
import './styles.scss';

const Settings = () => {
  return (
    <div className='settings'>
        <Header />
        This is the settings page.</div>
  )
};

export default Settings;